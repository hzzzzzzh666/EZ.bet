# 第三部分：核心功能实现与演示 - PPT内容

## 幻灯片1：用户管理模块 - 功能概述
- **主要功能**
  - 用户注册与登录
  - 用户信息管理
  - 角色权限分配
  - 密码修改与重置

- **技术实现**
  - JWT令牌认证
  - 密码MD5+盐值加密
  - 基于角色的访问控制(RBAC)
  - 前端表单验证

- **用户角色**
  - 管理员：系统全部权限
  - 教师：试题管理、考试管理、成绩管理
  - 学生：参加考试、查看成绩

## 幻灯片2：用户管理模块 - 关键代码实现
- **用户登录接口**
  ```java
  @PostMapping("/login")
  public Result login(@RequestBody UserLoginDTO userLoginDTO) {
      // 参数校验
      if (StringUtils.isEmpty(userLoginDTO.getUsername()) || 
          StringUtils.isEmpty(userLoginDTO.getPassword())) {
          return Result.error("用户名或密码不能为空");
      }
      
      // 查询用户
      User user = userService.findByUsername(userLoginDTO.getUsername());
      if (user == null) {
          return Result.error("用户不存在");
      }
      
      // 验证密码
      String encodedPassword = MD5Util.encode(userLoginDTO.getPassword(), user.getSalt());
      if (!encodedPassword.equals(user.getPassword())) {
          return Result.error("密码错误");
      }
      
      // 生成JWT令牌
      String token = JwtUtil.createToken(user.getId(), user.getUsername(), user.getRoleId());
      
      // 返回用户信息和令牌
      Map<String, Object> data = new HashMap<>();
      data.put("token", token);
      data.put("user", user);
      
      return Result.success(data);
  }
  ```

- **前端登录表单验证**
  ```javascript
  submitForm() {
    this.$refs.loginFormRef.validate(valid => {
      if (valid) {
        // 发送登录请求
        this.$http.post(this.API.login, this.loginForm).then(resp => {
          if (resp.data.code === 200) {
            // 保存令牌
            localStorage.setItem('token', resp.data.data.token);
            // 保存用户信息
            this.$store.commit('setUser', resp.data.data.user);
            // 跳转到首页
            this.$router.push('/dashboard');
          } else {
            this.$message.error(resp.data.message);
          }
        }).catch(error => {
          this.$message.error('登录失败，请稍后重试');
        });
      }
    });
  }
  ```

## 幻灯片3：用户管理模块 - 界面展示与操作演示
- **登录界面**
  - [在此处插入登录界面截图]
  - 用户名/密码输入框
  - 记住密码选项
  - 登录按钮

- **用户管理界面**
  - [在此处插入用户管理界面截图]
  - 用户列表展示
  - 搜索与筛选功能
  - 添加/编辑/删除按钮

- **用户信息编辑界面**
  - [在此处插入用户信息编辑界面截图]
  - 用户基本信息表单
  - 角色分配下拉框
  - 保存/取消按钮

## 幻灯片4：试题管理模块 - 功能概述
- **主要功能**
  - 试题增删改查
  - 试题分类管理
  - 试题导入导出
  - 试题库管理

- **支持题型**
  - 单选题
  - 多选题
  - 判断题
  - 填空题
  - 简答题

- **技术实现**
  - 富文本编辑器
  - 文件上传下载
  - 分页查询
  - 条件筛选

## 幻灯片5：试题管理模块 - 关键代码实现
- **试题添加接口**
  ```java
  @PostMapping("/add")
  public Result addQuestion(@RequestBody Question question) {
      // 参数校验
      if (StringUtils.isEmpty(question.getTitle()) || 
          StringUtils.isEmpty(question.getContent())) {
          return Result.error("题目标题和内容不能为空");
      }
      
      // 设置创建时间
      question.setCreateTime(new Date());
      question.setUpdateTime(new Date());
      
      // 保存试题
      boolean result = questionService.save(question);
      
      if (result) {
          return Result.success("添加试题成功");
      } else {
          return Result.error("添加试题失败");
      }
  }
  ```

- **前端试题添加表单**
  ```javascript
  submitForm() {
    this.$refs.questionFormRef.validate(valid => {
      if (valid) {
        // 根据题型处理答案
        if (this.questionForm.type === 'single') {
          this.questionForm.answer = this.singleChoiceAnswer;
        } else if (this.questionForm.type === 'multiple') {
          this.questionForm.answer = this.multipleChoiceAnswer.join(',');
        }
        
        // 发送添加试题请求
        this.$http.post(this.API.addQuestion, this.questionForm).then(resp => {
          if (resp.data.code === 200) {
            this.$message.success(resp.data.message);
            this.dialogVisible = false;
            this.getQuestionList(); // 刷新试题列表
          } else {
            this.$message.error(resp.data.message);
          }
        }).catch(error => {
          this.$message.error('添加试题失败，请稍后重试');
        });
      }
    });
  }
  ```

## 幻灯片6：试题管理模块 - 界面展示与操作演示
- **试题列表界面**
  - [在此处插入试题列表界面截图]
  - 试题列表展示
  - 按题型/分类筛选
  - 添加/编辑/删除按钮

- **试题添加界面**
  - [在此处插入试题添加界面截图]
  - 题型选择
  - 题目标题和内容
  - 选项和答案设置
  - 分值和分类设置

- **富文本编辑器**
  - [在此处插入富文本编辑器截图]
  - 支持图文混排
  - 支持公式编辑
  - 支持表格插入

## 幻灯片7：考试管理模块 - 功能概述
- **主要功能**
  - 考试创建与发布
  - 试卷组卷
  - 考试时间控制
  - 考试过程监控
  - 防作弊机制

- **组卷方式**
  - 手动组卷
  - 随机组卷
  - 智能组卷

- **防作弊机制**
  - 随机题目顺序
  - 随机选项顺序
  - 时间限制
  - 切屏检测

## 幻灯片8：考试管理模块 - 关键代码实现
- **考试创建接口**
  ```java
  @PostMapping("/create")
  public Result createExam(@RequestBody ExamCreateDTO examCreateDTO) {
      // 参数校验
      if (StringUtils.isEmpty(examCreateDTO.getTitle()) || 
          examCreateDTO.getStartTime() == null || 
          examCreateDTO.getEndTime() == null) {
          return Result.error("考试标题、开始时间和结束时间不能为空");
      }
      
      // 创建考试对象
      Exam exam = new Exam();
      BeanUtils.copyProperties(examCreateDTO, exam);
      exam.setCreateTime(new Date());
      exam.setUpdateTime(new Date());
      exam.setCreatorId(getCurrentUserId());
      
      // 保存考试
      boolean result = examService.save(exam);
      
      if (result) {
          // 保存考试试题关联
          if (examCreateDTO.getQuestionIds() != null && !examCreateDTO.getQuestionIds().isEmpty()) {
              examQuestionService.saveExamQuestions(exam.getId(), examCreateDTO.getQuestionIds());
          }
          return Result.success("创建考试成功");
      } else {
          return Result.error("创建考试失败");
      }
  }
  ```

- **前端考试创建表单**
  ```javascript
  submitForm() {
    this.$refs.examFormRef.validate(valid => {
      if (valid) {
        // 处理试题选择
        this.examForm.questionIds = this.selectedQuestions.map(q => q.id);
        
        // 发送创建考试请求
        this.$http.post(this.API.createExam, this.examForm).then(resp => {
          if (resp.data.code === 200) {
            this.$message.success(resp.data.message);
            this.dialogVisible = false;
            this.getExamList(); // 刷新考试列表
          } else {
            this.$message.error(resp.data.message);
          }
        }).catch(error => {
          this.$message.error('创建考试失败，请稍后重试');
        });
      }
    });
  }
  ```

## 幻灯片9：考试管理模块 - 界面展示与操作演示
- **考试列表界面**
  - [在此处插入考试列表界面截图]
  - 考试列表展示
  - 按状态筛选
  - 创建/编辑/发布按钮

- **考试创建界面**
  - [在此处插入考试创建界面截图]
  - 考试基本信息设置
  - 试题选择界面
  - 时间设置

- **考试进行界面**
  - [在此处插入考试进行界面截图]
  - 题目展示
  - 答题区域
  - 倒计时显示
  - 提交按钮

## 幻灯片10：成绩管理模块 - 功能概述
- **主要功能**
  - 自动阅卷
  - 成绩查询
  - 成绩统计
  - 成绩分析报告
  - 成绩导出

- **自动阅卷**
  - 客观题自动评分
  - 主观题辅助评分
  - 评分规则设置

- **成绩分析**
  - 平均分、最高分、最低分
  - 及格率、优秀率
  - 题目难度分析
  - 知识点掌握情况

## 幻灯片11：成绩管理模块 - 关键代码实现
- **自动阅卷接口**
  ```java
  @PostMapping("/autoGrade")
  public Result autoGrade(@RequestBody ExamRecord examRecord) {
      // 获取考试记录
      ExamRecord record = examRecordService.getById(examRecord.getId());
      if (record == null) {
          return Result.error("考试记录不存在");
      }
      
      // 获取考试试题
      List<ExamQuestion> examQuestions = examQuestionService.getQuestionsByExamId(record.getExamId());
      
      // 计算得分
      int totalScore = 0;
      for (ExamQuestion examQuestion : examQuestions) {
          Question question = questionService.getById(examQuestion.getQuestionId());
          String userAnswer = getUserAnswer(record.getId(), question.getId());
          int score = calculateScore(question, userAnswer);
          totalScore += score;
          
          // 保存答题记录
          saveAnswer(record.getId(), question.getId(), userAnswer, score);
      }
      
      // 更新考试记录
      record.setScore(totalScore);
      record.setStatus(2); // 已阅卷
      record.setUpdateTime(new Date());
      examRecordService.updateById(record);
      
      return Result.success("阅卷完成", totalScore);
  }
  ```

- **前端成绩展示**
  ```javascript
  getExamResults() {
    this.$http.get(this.API.getExamResults, {
      params: {
        examId: this.examId,
        userId: this.userId
      }
    }).then(resp => {
      if (resp.data.code === 200) {
        this.examResult = resp.data.data;
        this.initChart(); // 初始化图表
      } else {
        this.$message.error(resp.data.message);
      }
    }).catch(error => {
      this.$message.error('获取考试成绩失败，请稍后重试');
    });
  }
  ```

## 幻灯片12：成绩管理模块 - 界面展示与操作演示
- **成绩列表界面**
  - [在此处插入成绩列表界面截图]
  - 成绩列表展示
  - 按考试/学生筛选
  - 导出按钮

- **成绩详情界面**
  - [在此处插入成绩详情界面截图]
  - 学生答题情况
  - 每题得分情况
  - 总分展示

- **成绩分析图表**
  - [在此处插入成绩分析图表截图]
  - 分数分布图
  - 题目正确率
  - 知识点掌握情况

## 幻灯片13：系统亮点功能
- **实时考试监控**
  - 实时显示考试人数
  - 考试进度监控
  - 异常行为检测
  - 考试日志记录

- **智能组卷**
  - 基于难度系数组卷
  - 基于知识点组卷
  - 避免重复题目
  - 平衡试卷难度

- **成绩分析报告**
  - 个人成绩报告
  - 班级成绩分析
  - 知识点掌握分析
  - 学习建议推荐

- **移动端适配**
  - 响应式设计
  - 移动端优化
  - 触屏操作支持
  - 离线答题支持